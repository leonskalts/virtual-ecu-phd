# Preregistered CLO-DSF v7.2 development confirmation

DEVELOPMENT CONFIRMATION — NOT FINAL HOLDOUT. This protocol, scientific reference, selected parameters, study, evaluator and evidence maps are hashed before the first confirmation execution. There is no parameter search or train/validation split in this task. The completed Candidate 2 no-origin-discount ablation must first reproduce 262 detected, 202 correct origins, 60 UNKNOWN and zero wrong origins on its historical validation. Historical reuse is a reproduction gate, never new confirmation.

## Fixed algorithm and parameter provenance

Two independent frames: detection {NORMAL,ABNORMAL}; origin {MEMORY,TIMING,COMMUNICATION,SENSOR_CONTROL,ACTUATOR}. Reuse Candidate 2's exact positive runtime transformations and subset mapping. Every available strength e supports ABNORMAL with mass e and ignorance 1−e. Healthy/unavailable sources are vacuous; no artificial NORMAL absence mass. Origin supports, in channel order: TIMING; COMMUNICATION; MEMORY; {COMMUNICATION,SENSOR_CONTROL}; ACTUATOR; entire origin frame. A plant symptom is uninformative for origin. No new signal, singleton, fault-dependent threshold or truth-derived residual.

Evidence transformations: timing maximum of deadline/missed/late flags and clipped excess execution age over period+deadline; communication freshness failure/backward timestamp or clipped excess age above 300 ms; target deviation |target−92|/4; measured slew excess above 2.5 C/s divided by 2 or outside [−40,150]; max fan mismatch/.25 and pump mismatch/.20; measured temperature excess above 108 divided by 7. Clip strengths to [0,1]. Invalid measurements are unavailable, with the same rules as Candidate 2.

Load authoritative values from Candidate 2 selected_config.cfg, verifying its selection-record hash. Copy r_detection=1, lambda_temporal=.8, suspect_threshold=.35, confirmed_threshold=.5, confirmation_persistence=1, localization_threshold=.55, localization_margin_threshold=.15, ignorance_limit=.5. Remove origin discount and propagation modulation completely from the final decision code. Diagnostic episode history can be updated after inference, with the existing 3000 ms window, but cannot modify mass, score or decision. Observability remains structural: available sources, legitimate origin subsets, ignorance and abstention.

Fold each frame in timing, communication, memory/control, sensor/control, actuator, plant order, followed by its own discounted previous mass. Use Dempster normalization, K≥1−1e−12 flagged vacuous fallback. Preserve five-logical-origin pignistic cardinality in the unchanged 64-entry partition embedding. Detection reads only abnormal belief. Localization requires CONFIRMED plus score, margin and ignorance gates. UNKNOWN is intentional. Duplicate/backward observations are ignored. Cadence 100 ms. No tuning after this protocol is registered.

## Confirmation configurations and fairness

900 unique 120-second configurations: 720 fault cases (144 per origin) and 180 benign. Four new profiles, two onsets, three behaviors, multiple magnitudes/durations and three declared seeds. Seed varies across otherwise distinct configurations; deterministic/repeated settings are not treated as independent random trials. Permanent ignored duration and severity-less deadline/fan cases are normalized in the semantic audit; distinct onset offsets prevent duplicate configurations. Compare profile CONTENT plus physical fault semantics with all Candidate 1 and Candidate 2 train/validation settings before execution. No overlap or new duplicates are permitted. No outcome-based case selection.

All methods run online on the same observations, without intervention: final reference; Candidate 2 selected; Candidate 2 no origin discount; final with temporal retention zero as a declared ablation; frozen Candidate 1; matched Plain DS; OR .5; Weighted Sum using exactly Candidate 2's selected index/configuration; unchanged Hybrid; unchanged Timing Monitor (timing/benign reporting only). Weighted Sum is not recalibrated. Existing action methods remain observe-only. Original runtime/physics remain unchanged.

## Endpoints and statistics

Truth and injection onset are scoring metadata only, joined after runtime. Detection requires a NEW alarm edge at/after onset; preexisting alarms are not credited. Report pre-onset alarms separately. Primary localization endpoint is origin at that first alarm. Track first later localization, correctness, latency and whether it precedes plant manifestation. Keep detected UNKNOWN separate from missed cases.

Report all requested overall/per-origin/conditional detection, silent plant, benign alarms, precision/recall, median/P95 latency and pre/same/post plant counts. Report correct/all alarms, localization coverage, accuracy conditional on localization, wrong-origin count/rate, UNKNOWN, macro accuracy, confusion, margin/ignorance distributions and confirmed-UNKNOWN control/actuator/plant cohorts. Use descriptive 95% Wilson intervals for eligible proportions, always with numerator/denominator. This designed deterministic cohort has correlated configuration families; intervals are nominal binomial summaries, not population safety guarantees or validated independence assumptions.

Paired final versus Weighted Sum, OR and Candidate 2: both/only-final/only-baseline/neither on fault cases. Compute exact two-sided binomial McNemar conditional on discordant pairs only as exploratory statistics; if none, report no discordance/no test needed. With fewer than ten discordant cases, label low information and do not use significance for freeze. No statistical win is required. Group dependence limits inferential interpretation even at larger counts.

Identifiability is post hoc only. Hash the complete allowed runtime-observation CSV stream and separately the complete six-channel strength/availability stream across the full run; list every mixed-origin equivalence group. Also inspect exact observation vectors at first alarms, distinguishing snapshot coincidence from full-history equivalence. Round nothing for exact groups. Whole-history equivalence with different origins proves ambiguity for that observed stream; evidence-stream equivalence proves ambiguity for this feature map, not all richer observers. Pre-fault shared prefixes and merely instantaneous matches do not prove full-history impossibility. If no empirical sensor/communication collisions exist, say so; retain the structural composite-subset argument and hidden-label invariance test without inventing empirical collisions.

## Freeze, optimization and integrity

After confirmation, only FREEZE or DO NOT FREEZE is allowed. Freeze requires scientific coherence, regression/isolation passes, correct independent frames, meaningful selective localization, acceptable observed benign behavior and a defensible measured capability beyond binary pooling. No arbitrary percentage gate; do not demand a Weighted Sum detection win. A fundamental failure means do not freeze or retune.

If justified, first write the mathematical frozen contract and complete JSON. Only then attempt an exact implementation optimization; retain the reference. Replay EVERY archived confirmation observation through both implementations. Require identical state, alarm/origin decisions, localization validity and both timestamps; require bit-identical masses/output where feasible, otherwise document a numeric tolerance ≤1e−12 and reject every decision discrepancy. No new confirmation configurations or threshold changes.

Benchmark legacy, Candidate 1, Candidate 2, final reference and optimized final with 31 interleaved host samples after three warmup cycles; report state, mass/evidence sizes and binary text/data/BSS. Logging and process costs are explicit; no embedded WCET or lightweight claim. Final package hashes the scientific contract, configuration, source, dependencies, maps, protocol and study. Preserve every historical artifact and user session; no staging/commit/push and no final holdout.

Statistics references: [NIST Wilson interval](https://www.itl.nist.gov/div898/handbook/prc/section2/prc241.htm) and [R paired McNemar definition](https://www.stat.ethz.ch/R-manual/R-devel/library/stats/html/mcnemar.test.html). Exact small-discordance tails are computed directly from the binomial distribution; no asymptotic significance claim is made for tiny cohorts.
