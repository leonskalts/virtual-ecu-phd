Read NEXT_SESSION_PROMPT.txt and continue from the current repository state.

Before making changes:
1. Run git status --short.
2. Inspect scripts/virtual_ecu_gui.py.
3. Inspect README.md.
4. Inspect the current Multi-Fault Scenario Builder and Scenario Timeline implementation.

Then refine the Multi-Fault Scenario Timeline in the GUI so it looks clean and presentation-friendly.

Keep the C simulator unchanged.
Keep the GUI in Python/Tkinter.
Do not add heavy dependencies.

The current Scenario Timeline works functionally, but it looks visually crowded and not polished enough for thesis/demo use.

Please do all of the following:

1. Redesign the Scenario Timeline visually without removing the feature.
2. Make it cleaner, simpler, and easier to read at a glance.
3. Reduce clutter inside the timeline area:
   - remove unnecessary explanatory text from inside the drawing area
   - avoid too much small text inside the chart itself
   - keep only the most important labels visible
4. Improve spacing and hierarchy:
   - give the timeline more breathing room
   - increase margins/padding
   - align the event rows more cleanly
5. Make the timeline feel more like a presentation card and less like a debugging canvas.
6. Improve the time axis:
   - make start/end markers cleaner
   - keep labels readable
   - avoid overlapping labels
7. Improve event rendering:
   - cleaner bars
   - clearer permanent-fault visualization
   - better row separation
   - more consistent colors
8. Keep the live update behavior when events are added, updated, removed, reordered, or loaded from presets.
9. Keep the existing scenario builder workflow and buttons intact.
10. Do not break:
   - custom presets
   - run scenario actions
   - comparison flow
   - figures/export workflow

Also provide:
- what visual changes were made
- why the timeline now looks better
- whether it is now suitable for thesis/demo screenshots

Finally run verification:
- make
- python3 -m py_compile scripts/virtual_ecu_gui.py
- a smoke test if possible
