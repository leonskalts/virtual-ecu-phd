Read NEXT_SESSION_PROMPT.txt and continue from the current repository state.

Before making changes:
1. Run git status --short.
2. Inspect scripts/virtual_ecu_gui.py.
3. Inspect README.md.
4. Inspect presets/recent_results.json if present.
5. Summarize the current GUI usability features.

Current important GUI usability features expected:
- Quick Start / Guided Use panel
- Showcase / Demo Presets
- Load Existing Results
- Custom single-fault experiments
- Multi-fault scenario builder
- Scenario Timeline
- Recent Results / Recent Comparisons
- Comparison Summary / Comparison Figures / Batch Results
- Propagation Evidence and fault-path visualization
- exportable reports and snapshots

Next task idea:
Add a lightweight Favorites / Pinned Comparisons feature to the virtual ECU GUI.

Requirements:
- Keep the C simulator unchanged.
- Keep the GUI in Python/Tkinter.
- Do not add heavy dependencies.

Goal:
Let the user pin important comparison pairs for quick reuse, separate from Recent Results.

A favorite item should store at least:
- title
- left result source
- right result source
- optional short note/description

The GUI should let the user:
- add the current comparison as a favorite
- load a favorite with one click
- remove a favorite
- optionally rename/edit the display title

Reuse the existing comparison-loading pipeline so favorites update:
- Comparison Summary
- Comparison Figures
- Propagation Evidence
- Fault Path visualization
- export/report workflow

Store favorites in a simple deterministic format such as:
presets/favorite_comparisons.json

Keep the workflow compact and presentation-friendly.
Do not clutter the interface.

Update README with a short explanation of favorites and where they are stored.

First summarize the cleanest place in the GUI to add this feature before editing files.
