Continue from the current repository state.

This project is a virtual ECU framework for automotive thermal management, now developed as a cross-layer hardware-origin fault propagation platform connected to VLSI and automotive electronics.

Current implemented fault classes:
- sensing-path faults
- timing/communication-path faults
- actuation-path faults
- computation/memory-path faults

Current built-in campaigns include:
- baseline
- sensor_bias_only
- sensor_interface_intermittent
- stale_sensor_data_only
- stale_sensor_data_hot_stress
- pump_degraded_only
- fan_stuck_only
- fan_stuck_hot_stress
- calibration_memory_corruption
- paper_default

Current important project state:
- paper-results scripts exist
- batch runner exists
- batch analysis and claim-focused analysis scripts exist
- GUI supports campaign comparison
- GUI supports exportable comparison reports
- GUI supports results snapshots
- GUI supports batch-results viewing
- Presentation Mode was refined so it now affects only the Comparison Figures view, not Comparison Summary or Batch Results

What I want you to do first:
1. Inspect the current repository state.
2. Summarize what is already implemented now.
3. Identify the best next improvement that would make the project stronger as a research/demo platform, without changing the C simulator into a low-level hardware simulator.
4. Prefer the next step to be practical and visible, not just paper-writing help.

Important constraints:
- keep the C simulator architecture simple
- keep the project modular
- keep it compilable with gcc in WSL
- keep Python tooling lightweight
- do not add heavy dependencies
- do not forget the VLSI + automotive cross-layer framing

When you answer, first give:
- current repo summary
- best next improvement
- why that improvement is better than other possible next steps